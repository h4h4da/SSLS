
@model SSLS.WebUI.Models.BooksListViewModel

<div class="col-lg-6">
    <div class="input-group">
        @using (Html.BeginForm())
        {


            <div class="input-group">
                @Html.TextBoxFor(vm => vm.SearchBookStr, new { @class = "form-control" })
                <span class="input-group-btn">
                    <button class="btn btn-default" type="submit">Search</button>
                </span>
            </div>

            @*<div class="input-group">
                @Html.TextBoxFor(vm => vm.SearchBookStr, new { @class = "form-control" })
                <button type="submit" class="input-group-btnt">Submit</button>

            </div>
            <div class="form-group">
               
                <input type="text" class="form-control" placeholder="Search">
            </div>*@
            
           
            @*<span class="input-group-btn">
                <button class="btn btn-default" type="submit">Search</button>
            </span>*@
        }
    </div>
</div>


<table class="table">
    <tr>
        <th>
            书籍名称
        </th>
        <th>
            出版社
        </th>
        <th>
            作者
        </th>

    </tr>

    @foreach (var item in Model.Books)
    {

        @Html.Partial("BookSummary", item);

    }
</table>


<div class="btn-group pull-right">
    @Html.PageLink(Model.paginginfo, x => Url.Action("List", new { categoryId = Model.CurrentCategoryId, page = x }))
</div>
